spring:
  profiles:
    active: testing
    include:
      - email
      - resources

  liquibase:
    contexts: testing

logging:
  level:
    org.springframework.*: info
    io.realmarket.propeler.*: info