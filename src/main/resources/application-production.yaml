spring:
  profiles:
    active: production
    include:
      - email
      - resources
  liquibase:
    contexts: production

platform.name: RealMarket

logging:
  level:
    org.springframework.*: info
    io.realmarket.propeler.*: info